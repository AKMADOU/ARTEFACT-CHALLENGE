# Installation (Python 3.10+)
pip install dbt-core dbt-duckdb

# Cloner / dézipper le projet
cd dbt_project



python setup_seeds.py \
  --starter air_cote_divoire_starter_dataset.xlsx \
  --synthetic data/synthetic \
  --seeds dbt_project/seeds


export DBT_PROFILES_DIR=.
dbt seed && dbt run && dbt test



# Depuis ton dossier projet (là où est dbt_project/)
pip install -r dashboard/requirements.txt

# S'assurer que airci.duckdb est généré
cd dbt_project && export DBT_PROFILES_DIR=. && dbt seed && dbt run

# Lancer le dashboard
cd ..
streamlit run dashboard/app.py




Phase 4 — MCP server (Jour 4)
Petit serveur Python qui expose 4-5 outils : query_route_metrics, get_at_risk_customers, search_reviews_by_route (RAG sur les reviews), recommend_budget_allocation.
Phase 5 — Livrables & vidéos (Jour 5)
README, data dictionary, diagramme, write-up, vidéo dashboard, vidéo AI.